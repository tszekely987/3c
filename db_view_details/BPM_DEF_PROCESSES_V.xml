<ClarityDBViewQuery>
  <QueryResult code="BPM_DEF_PROCESSES_V">
    <indexes/>
    <text_length>270</text_length>
    <text>SELECT 
  c.name,
  c.description,
  c.language_code,
  p.id,p.PROCESS_CODE, p.CREATED_DATE, p.CREATED_BY, p.LAST_UPDATED_DATE, p.LAST_UPDATED_BY FROM 
  BPM_DEF_PROCESSES p,
  CMN_CAPTIONS_NLS c
WHERE 
  p.id = c.pk_id and
  c.table_name = 'BPM_DEF_PROCESSES'
 
 
 
 
 </text>
  </QueryResult>
</ClarityDBViewQuery>

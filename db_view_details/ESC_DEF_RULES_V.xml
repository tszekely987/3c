<ClarityDBViewQuery>
  <QueryResult code="ESC_DEF_RULES_V">
    <indexes/>
    <text_length>1745</text_length>
    <text>SELECT 
  R.ID,
  R.OBJECT_TYPE_CODE,
  C.NAME OBJECT_TYPE_NAME,
  C.DESCRIPTION OBJECT_TYPE_DESCRIPTION,
  R.PARENT_OBJECT_TYPE_CODE,
  R.PARENT_OBJECT_ID,
  R.PATH_TYPE_CODE,
  PATH.NAME PATH_NAME,
  R.PATH_ID,
  (SELECT UNIQUE_NAME FROM PRJ_OBS_TYPES WHERE ID = R.PATH_ID AND R.PATH_TYPE_CODE = 'ESC_PATH_OBS_HIERARCHY') OBS_CODE,
  (SELECT NAME FROM PRJ_OBS_TYPES WHERE ID = R.PATH_ID AND R.PATH_TYPE_CODE = 'ESC_PATH_OBS_HIERARCHY') OBS_NAME,
  (SELECT UNIQUE_NAME FROM SRM_RESOURCES WHERE USER_ID = R.PATH_ID AND R.PATH_TYPE_CODE = 'ESC_PATH_RESOURCE') RESOURCE_CODE,
  (SELECT FULL_NAME FROM SRM_RESOURCES WHERE USER_ID = R.PATH_ID AND R.PATH_TYPE_CODE = 'ESC_PATH_RESOURCE') RESOURCE_NAME,  
  R.INITIAL_PERIOD_INTERVAL,
  R.INITIAL_PERIOD_TYPE_CODE,
  INI_PERIOD.NAME INITIAL_PERIOD_NAME,  
  R.SUBSEQUENT_PERIOD_INTERVAL,
  R.SUBSEQUENT_PERIOD_TYPE_CODE,
  SUB_PERIOD.NAME SUBSEQUENT_PERIOD_NAME,
  R.AI_W_DUE_DATE_ACTION_CODE,
  R.AI_WO_DUE_DATE_ACTION_CODE,  
  R.MAX_NO_OF_LEVELS,
  R.IS_ACTIVE,
  R.CREATED_DATE,
  R.CREATED_BY,
  R.LAST_UPDATED_DATE,
  R.LAST_UPDATED_BY,
  C.LANGUAGE_CODE
FROM 
  ESC_DEF_RULES R,
  CMN_CAPTIONS_NLS C,
  CMN_LOOKUPS_V PATH,
  CMN_LOOKUPS_V INI_PERIOD,
  CMN_LOOKUPS_V SUB_PERIOD
WHERE
  C.TABLE_NAME = 'ESC_DEF_RULES' AND
  C.PK_ID = R.ID AND
  PATH.LOOKUP_TYPE = 'ESC_PATH_TYPE' AND
  PATH.LOOKUP_CODE = R.PATH_TYPE_CODE AND
  C.LANGUAGE_CODE = PATH.LANGUAGE_CODE AND
  INI_PERIOD.LOOKUP_TYPE = 'ESC_PERIOD_TYPE' AND
  INI_PERIOD.LOOKUP_CODE = R.INITIAL_PERIOD_TYPE_CODE AND
  PATH.LANGUAGE_CODE = INI_PERIOD.LANGUAGE_CODE AND
  SUB_PERIOD.LOOKUP_TYPE = 'ESC_PERIOD_TYPE' AND
  SUB_PERIOD.LOOKUP_CODE = R.SUBSEQUENT_PERIOD_TYPE_CODE AND
  PATH.LANGUAGE_CODE = SUB_PERIOD.LANGUAGE_CODE
 
 
 
 
 </text>
  </QueryResult>
</ClarityDBViewQuery>

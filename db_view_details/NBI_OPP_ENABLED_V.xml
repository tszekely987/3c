<ClarityDBViewQuery>
  <QueryResult code="NBI_OPP_ENABLED_V">
    <indexes/>
    <text_length>1108</text_length>
    <text>SELECT   INV.ID PROJECT_ID,
		 COMPANY.ID CUSTOMER_ID,
         COMPANY.COMPANY_NAME CUSTOMER,
         COMPANY.COMPANY_ID CUSTOMER_CODE,
            --'Getting account number';
         B.ACCOUNT_MANAGER ACCOUNT_EXECUTIVE_ID,
         B.REVENUE_CURRENCY_CODE,
         B.REVENUE,
         R.LAST_NAME AE_LAST_NAME,
         R.FIRST_NAME AE_FIRST_NAME,
     /*     CMN_EXCHANGERATE_PKG.CMN_GET_ONLYCONVERTED_VAL_FCT (
            SYSDATE,
            B.REVENUE_CURRENCY_CODE,
            P_CURRENCY_CODE,
            B.REVENUE,
            P.LABOR_EXCHANGE_RATE_TYPE
         ) OPP_REVENUE, */
         B.STAGE OPP_STAGE,
         B.PROBABILITY OPP_PROBABILITY,
         B.EXPECTED_CLOSE_DATE OPP_EXPECTED_CLOSE_DATE,
         B.STATUS OPP_STATUS,
         B.TRK_PIPELINE_REPORT OPP_TRACK_ON_PIPELINE,
         P.LABOR_EXCHANGE_RATE_TYPE
FROM     SRM_COMPANIES COMPANY
JOIN     BIZ_OPP_GEN_PROPERTIES B ON B.COMPANY_ID = COMPANY.ID
JOIN     INV_INVESTMENTS INV ON INV.ID=B.ID
LEFT OUTER JOIN SRM_RESOURCES R ON B.ACCOUNT_MANAGER = R.ID
LEFT OUTER JOIN PAC_MNT_PROJECTS P ON B.ID = P.ID
WHERE    B.ID = INV.ID</text>
  </QueryResult>
</ClarityDBViewQuery>

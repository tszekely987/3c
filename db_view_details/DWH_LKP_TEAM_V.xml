<ClarityDBViewQuery>
  <QueryResult code="DWH_LKP_TEAM_V">
    <indexes/>
    <text_length>311</text_length>
    <text>SELECT t.prid TEAM_KEY,
       CASE WHEN prj.PRISROLE = 1 THEN NVL(t.REQUIREMENT_NAME, SUBSTR(srm.FULL_NAME,1,100))
            ELSE srm.FULL_NAME END REQUIREMENT_NAME
FROM   PRTEAM t
       INNER JOIN PRJ_RESOURCES prj ON t.PRRESOURCEID = prj.PRID
       INNER JOIN SRM_RESOURCES srm ON t.PRRESOURCEID = srm.ID</text>
  </QueryResult>
</ClarityDBViewQuery>

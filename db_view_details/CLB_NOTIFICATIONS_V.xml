<ClarityDBViewQuery>
  <QueryResult code="CLB_NOTIFICATIONS_V">
    <indexes/>
    <text_length>279</text_length>
    <text>SELECT   RECEIVER_ID,
  EVENT_TYPE,
  COUNT(*) AS EVENT_TYPE_COUNT
FROM   CLB_NOTIFICATIONS
GROUP BY  RECEIVER_id,EVENT_TYPE
UNION
SELECT   RECEIVER_ID,
  'NOTIFICATION_ALL_TYPE' EVENT_TYPE,
   COUNT(*) AS EVENT_TYPE_COUNT
FROM   CLB_NOTIFICATIONS
GROUP BY  RECEIVER_ID
 
 
 
 
 </text>
  </QueryResult>
</ClarityDBViewQuery>

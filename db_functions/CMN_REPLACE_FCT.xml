<?xml version="1.0" encoding="UTF-8" standalone="no"?><QueryResult xmlns="http://www.niku.com/xog/Query" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
      <Code>capture3c.get_db_function</Code>
      <Records>
        <Record>
          <code>CMN_REPLACE_FCT</code>
          <parallel>NO</parallel>
          <definition>
  CREATE OR REPLACE EDITIONABLE FUNCTION "PPM"."CMN_REPLACE_FCT" (strbase IN VARCHAR2,position IN NUMBER,rmlength
IN NUMBER,strnew IN VARCHAR2)RETURN VARCHAR2
 AS
   strresult VARCHAR2(4000);

  strbase_length INTEGER;
 BEGIN
   strbase_length:=LENGTH(strbase);

   IF( (position &lt; 0) OR (rmlength &lt; 0) OR (position &gt;strbase_length))THEN

      strresult :=NULL;
       RETURN strresult;
     ELSE
       strresult
:=SUBSTR(strbase,1,position-1)||strnew||SUBSTR(strbase,position + rmlength);

      RETURN strresult;
     END IF;
 END;
 
 
 
 
 </definition>
        </Record>
      </Records>
      <Slice>
        <Number>0</Number>
        <Size>1</Size>
        <Total>1</Total>
      </Slice>
    </QueryResult>